name: Benchmarks

on:
  push:
    branches: [ main ]
  pull_request:

permissions:
  contents: write

jobs:
  bench:
    name: Run benches and commit markdown
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          fetch-depth: 0

      - name: Install dfx and IC SDK
        uses: dfinity/setup-dfx@main

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Install mops and PocketIC
        run: |
          npm install -g ic-mops @dfinity/pic
          mops --version

      - name: Install dependencies (mops)
        run: mops install

      - name: Setup Motoko toolchain (mops)
        run: mops toolchain init

      - name: Run benches to markdown
        run: |
          mops bench > benchmark.md

      - name: Commit benchmark.md if changed
        run: |
          if git diff --quiet -- benchmark.md; then
            echo "No changes in benchmark.md"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add benchmark.md
            git commit -m "chore(bench): update benchmark.md [skip ci]"
            git push
          fi
